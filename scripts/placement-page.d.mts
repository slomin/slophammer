// Hand-written declarations so the TypeScript Playwright spec can import the
// same renderer `serve-test-page.mjs` uses, without enabling allowJs.
export function renderPlacementPage(): string
export function renderPlacementFrame(): string
